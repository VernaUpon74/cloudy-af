# Handoff — Cloudy AF continuation (2026-09-15)

- **Repo (primary):** `/var/home/j/cloudy-af` — branch `master`, HEAD
  `5000e3c` (2026-09-12, "fix(appimage): node-hid mutex-patch verification
  gate…"). Version 1.2.0 (package.json + Cargo.lock synced at `019bf25`).
- **Mirror:** `/var/home/j/Documents/GitHub/cloudy-af` — used for pushes that
  trigger GitHub Actions (macOS build); keep the two in sync before pushing.
- **Knowledge graph:** MCP project `var-home-j-cloudy-af`, status `ready`
  (21,320 nodes / 30,261 edges). Only parse_partial flags live in
  `reverse-skill/` scripts and `resources/definitions/ArcticFox.xml:1-10`;
  app code (src/, src-tauri/, sidecar/) is clean. Re-index after large
  changes; call `check_index_coverage` before exhaustive/negative claims.

## Read these first (authoritative — do not duplicate their content)
- `/var/home/j/cloudy-af/AGENTS.md` — conventions: i18n rules, delegation
  hardware profile, node-hid patch + toolbox rebuild, manual AppImage
  extract/repack procedure, flatpak `--disable-rofiles-fuse`, toolbox Rust
  build command.
- `/var/home/j/cloudy-af/docs/goals.md` — open-item ledger with per-item
  root-cause write-ups (macOS CI, Dependabot glib, T2/T3 delegate results).
- Also: `CHANGELOG.md`, `todo.md`, `docs/keyboard-shortcuts.md`,
  `REVIEW_DIRECTIVE.md` (UI-review prompt template).

## Working tree state (uncommitted — decide, then commit or discard)
4 tracked modifications/deletions; everything else is untracked clutter
(firmware bins, downloaded HTML, logs — intentional; the stray corrupt file
`.py\030\021\027\021` is safe to remove).

1. **`index.html` + `public/i18n/en.json`** — Puff Time label rework:
   `Stats.PuffTimeHHMMSSLabel` ("Puff Time (HH:MM:SS)") → `Stats.PuffTimeLabel`
   ("Puff Time:") on the Stats Puff Time row (~line 2408). Intent: label no
   longer claims a fixed HH:MM:SS format (device-default display in 1.2.0).
   **i18n VIOLATION (verified with scripts/check-i18n.py):** the new key
   exists in `en.json` ONLY — all 16 other locales are at 386 keys vs en 387.
   Per AGENTS.md a missing key renders BLANK at runtime. Fix = add
   `Stats.PuffTimeLabel` (translated) to all 17 files in `public/i18n/`,
   then `python3 scripts/check-i18n.py` must pass. (The old HHMMSS key was
   in 0 locale files, so that row rendered blank before this change too.)
2. **`src/renderer-monitor.js`** — Device Monitor chart REVERTED from the
   1.19.0 dual y-axis (resistance on right axis) back to a single fixed
   y-axis 0–640, tickInterval 100; `yAxis: 1` dropped from
   `resistance`/`realResistance` in `SENSORS` (~lines 48–57, 161–175).
   **This UNDOES a feature recorded done in goals.md.** Confirm the desired
   final state with the user; then commit + update goals.md, or revert.
3. **`scripts/build-appimage.sh` — DELETED in the working tree** (177 lines,
   last touched by `072f3bd`) — **superseded, intentional**: the new root
   `build.sh` (untracked, Sep 13 01:20) is a unified interactive builder
   (Flatpak + AppImage targets, Docker with rootless-Podman socket
   fallback, experimental-patches option) that re-implements the AppImage
   pipeline via linuxdeploy. Follow-up: repoint `package.json`'s dangling
   `"appimage:build": "bash scripts/build-appimage.sh"` at the new flow (or
   drop the entry). Companion untracked docs: `BUILDING_WITH_DOCKER.md`,
   `BUILD_FLATPAKS_INSTRUCTIONS.md`, `Dockerfile`, `.dockerignore`.

## Recent work (reconstructed from edit timestamps + diffs + delegate logs; last commit `5000e3c`, Sep 12 13:12)
The uncommitted tree is one continuing session on top of the 1.2.0 release.
Two interleaved threads (mtimes are context clues, not proof):

1. **Sep 12 18:00–18:03 — Puff Time label rework** (`index.html`,
   `en.json`): follow-up to `019bf25` ("seed puffs-time display from config
   after load") and the 1.2.0 change that renders Puff Time in
   device-default format — with that, the old `Stats.PuffTimeHHMMSSLabel`
   ("Puff Time (HH:MM:SS)") misdescribed the field, so it became
   `Stats.PuffTimeLabel` ("Puff Time:"). Left half-done: en-only key (the
   i18n violation above) — the task was interrupted mid-flight.
2. **Sep 12 18:34 — Device Monitor chart revert** (`renderer-monitor.js`,
   last code edit of the session): dual y-axis (1.19.0) → single fixed
   0–640 axis, tickInterval 100. Series entries still carry a `yAxis` field
   (defaulted to 0 at :219), so restoring the resistance right-axis is a
   small diff if the revert turns out to be unwanted.
3. **Sep 13 00:23–01:20 — build-tooling night**: `local-flags.json` patches
   flag ON locally + new root `build.sh` replacing
   `scripts/build-appimage.sh`, with `BUILDING_WITH_DOCKER.md` /
   `BUILD_FLATPAKS_INSTRUCTIONS.md` / `Dockerfile` / `.dockerignore`
   companions (all untracked).

**RE / emulation thread (the deep-work line, Sep 4 → Sep 13):**

5. **Sep 4–10 — Phase 3 "Animation Effects": RE + emulator + effect
   runtime** (plan `docs/superpowers/plans/2026-09-04-animation-effects-phase3.md`,
   spec `docs/superpowers/specs/2026-08-19-firmware-animation-pipeline-design.md`):
   - Charge-screen render RE'd in af_190602 (decrypted, VandalProof):
     composer `FUN_0000b53c` @ `0xb53c`, inner 128×64 framebuffer fill at
     `0x8cd0` (emulator entry `0x8cd1`), buffer @ `0x20002758` —
     `resources/re/af_190602-render.md` (+ `resources/boot/af_190602.json`,
     `resources/animations/af_190602.json` descriptors).
   - Thumb emulator substantially extended to Thumb-2 for this:
     `src-tauri/src/firmware/emu/` — `cpu.rs` (83 KB) + `thumb.rs` (64 KB,
     both last touched Sep 10 14:34) with ~40 instruction-semantics unit
     tests each visible in the graph (flags, alignment faults, ACL
     violations, load/store round-trips), plus `bus.rs` / `harness.rs`.
   - Effect stack: `src-tauri/src/firmware/anim/` — `asm.rs` (Thumb
     bytecode builder, Sep 9) + `effects.rs` (Sep 10) emitting ~few-hundred-
     byte integer-math effects into a firmware code cave behind a 2-halfword
     branch detour at the render hook.
   - **Gate status (proof in logs):** `emu_test_anim.log` shows
     `test_af_190602_animation_frames` PASSING with gradient/center/diagonal
     effects executing in-emulator ("gradient: ok … center: ok … diagonal:
     ok") — the Phase 3 pre-flash gate is green. `render_gate` remains
     `#[ignore]` (needs the gitignored decrypted artifact).
6. **Sep 12 11:53 — dispatcher differential RE** (`resources/re/af_190602-dispatcher-analy.md`,
   committed with resources.rs): screen dispatcher & power-off effect
   analysis. Contains the reproducible Ghidra 12.1.2 headless pipeline
   (tooling in `scratch-stash/DecryptProject/`, JDK 21, AFSamples project,
   10 programs) and **hard-won gotchas**: one script dir per run (OSGi
   `ClassNotFoundException` otherwise), no `var` params in Ghidra scripts,
   pre-compile with javac to see real errors, fresh `sN/` + delete
   `~/.config/ghidra/.../osgi` per run. Queryable disassembly text dump:
   repo-root `af190602_dis.txt`.
7. **Sep 13 08:33–08:40 — NFirmware/arcticfox reference import** (same
   morning as the patches flag): `nfe_decomp/` (decompiled NCore /
   NFirmware / NToolbox trees), `af_pristine/` (extracted
   `arcticfox-11.0.3.tgz` — matches the sidecar's patched arcticfox@11.0.3),
   and `af_patchgen/` — a small git repo whose only commit ("pristine",
   08:40) snapshots `node_modules/arcticfox/index.js` (1,185 lines, the JS
   firmware patch engine) as a pristine baseline. `af_verify/` is an empty
   stub. Context: the Patches tab (local-flags `patches: true`, enabled the
   same night) and `patch.rs` — this looks like groundwork for
   patch-generation parity work (Rust `patch.rs` vs the arcticfox JS
   engine) or a JS-driven patchgen tool on top of the pristine copy.
   `nfe_decomp/` serves the same purpose for NFirmwareEditor parity checks
   (strings/resource-pack round-trips, open work #3).
8. **Sep 13 19:51 — UI review staged, report lost**: `REVIEW_DIRECTIVE.md`
   (Vercel-rubric Playwright review prompt) is the newest repo file; its
   intended output (`/tmp/ui-review-*.md`) sat on the since-cleared /tmp,
   so the review never landed. Re-run it via the browser-automation skill —
   it would also have caught the blank Puff Time label.

**Validation thread (Sep 13–14, reconstructed from Cline session logs +
`Documents/GitHub/cloudy-af/docs/validation-handoff.md` — this thread lives
in the MIRROR, not the primary repo; see the drift warning below):**

9. **Sep 13 — §4 static cave audit — DONE.** `src-tauri/src/bin/anim_patch_dump.rs`
   (dumps patched firmware + modification metadata → `/tmp/audit/{stock,
   gradient,center,diagonal}.bin` + `*.mods.json`; rollback byte-exact for
   all 3 effects) and `scripts/audit-cave.py` (capstone static audit:
   diff confinement vs stock, hook must be a single `b.w` to cave_start,
   contiguous decode to the terminating `b.w hook_resume`, instruction
   whitelist, store discipline, branch bounds, literal-pool containment,
   config byte ∈ {2,3,4} on an erased cell). **Result: PASS ×3.**
   Cave layout worth knowing before touching the emitter: code runs
   contiguous from cave_start to the terminator, then an optional `0x46C0`
   alignment nop, then a 4-byte-aligned literal pool (config byte addr
   `0x0001F7F0`, phase global, framebuffer base, sine-table base, MMIO reg,
   saved-reg addrs) — `body_len` includes the pool, so linear disassembly
   past the terminator desyncs.
10. **Sep 13 — §3 convergence + goldens — DONE.** `emu_test.rs` gained
    self-goldening in `test_af_190602_animation_frames` (writes
    `AF_fw/goldens/{name}_phase{k}.bin`, 12 files, byte-exact compare when
    present) and `#[ignore]` gate
    `test_af_190602_patched_converges_when_gated_off` (gradient patch with
    config stub 0 vs stock harness, identical drive → framebuffer byte-exact
    after every call, phase global stays 0). Two consecutive runs pass.
11. **Sep 13 — §5 uhid double + flasher gate — WRITTEN + 5 LIVE PASSES.**
    `scripts/uhid_ldrom.py` (stdlib-only `/dev/uhid` LDROM double: VID 0416
    PID 5020, manufacturer `Nuvoton`, serial suffix `-uhid`, serves 0x35
    dataflash with boot flag user[9]=1, PID `M041` @ user offset 312, fw ver
    110 @ user 256; 0xC3 consumes arg2 bytes with `--nak-delay-ms` /
    `--die-at-offset`; 0xB4 reboot is a no-op — the boot flag is cleared by
    the completed 0xC3 stream, not by 0xB4). Rust gate
    `test_flash_uhid_double` in `flasher_test.rs` — **the real Pico 25 must
    be UNPLUGGED for this test** (it asserts exactly one matching device).
    Hard-won detail: the CREATE2 struct must match `linux/uhid.h` exactly
    (packed, sizeof 4368, `rd_data` at +280, `rd_size` @ +260 sent or the
    kernel rejects with EINVAL); the report descriptor has ONE usage so the
    `linux-native` backend yields exactly one DeviceInfo. The kernel gives
    NO backpressure on OUT reports (31-deep ring, silently drops beyond) —
    keep host bursts under that. Honestly: value for animation work is
    **LOW/orthogonal** — it gates the *flasher protocol*, not the effects;
    keep it as a delivery-path safety net, not as animation validation.

12. **Sep 14 — §2 full-boot per-PID dispatch emulation — IN PROGRESS, and
    it does NOT compile yet.** Plan:
    `docs/superpowers/plans/2026-09-14-fullboot-pid-dispatch-emulation.md`
    (5 tasks). RE findings from the decrypted image:
    `0x302C` is NOT the boot dispatch (no BL callers); PID→panel happens at
    the dispatcher **`0xD684`**, reached via `dataflash_load_settings`
    (`0x16C64`, listed in the boot descriptor's `main.init_calls`); the TBH
    at `0x3040` is a 37-entry indirect table into `0x30A0+`. The hang target
    `0x2FF8` is a real infinite loop — reaching it means dispatch failure
    (the Pico Dual hang mode). Trick that avoids new Bus plumbing: place the
    2048-byte dataflash at `0x1F000` in the flash image (the existing flash
    read path serves it); PID sits at `img[0x1F13C..0x1F140]`. Descriptor
    caveat: the boot json uses a DIFFERENT schema than the emulator's
    `Descriptor` (no `render_entry`/`display_buffer`), so load
    `resources/animations/af_190602.json` as the Descriptor and hardcode the
    reset/SP/hang constants for the first cut.
    **Three known compile defects in `emu_test.rs` (fix these first):**
    (a) missing closing braces after
    `test_af_190602_patched_converges_when_gated_off` — the Task 1/2 block
    was inserted INSIDE that fn, so the helpers + pin test are nested and
    never collected; needs `    }` + `}` before the "Build a combined
    firmware+dataflash image" comment; (b) `boot_until_settle` has a bogus
    `use super::cpu::Cpu;` — in `tests/emu_test.rs` `super` is
    `crate::firmware::tests` and `Cpu` is never referenced by name; drop it;
    (c) `Harness::RETURN_SENTINEL` is the wrong path — it is a module const
    in `firmware::emu::harness`
    (`crate::firmware::emu::harness::RETURN_SENTINEL`).
    Then plan Tasks 2–3: `test_af_190602_boot_dispatch_by_pid` (`#[ignore]`,
    same artifact gate; PID matrix M041/M065/M077/M045/M038/M037/M095/M105/
    M064 → Ok(true), M177 → documented skip (STM32 line in a Nuvoton image),
    negatives `XXXX`/`0xFFFFFFFF`/`0x00000000` → Ok(false) via `0x2FF8`;
    first-cut budget 10M instructions). Task 4 (watchdog model) is a
    stretch; Task 5 is doc bookkeeping. Run:
    `toolbox run -c arcticfox-build sh -c 'cd src-tauri && cargo test
    --offline --release --lib firmware::tests::emu_test -- --ignored'`.
    Known doc drift to reconcile: goals.md's uhid entry still says "NOT YET
    RUN LIVE" while item 11 records 5 live PASS runs — verify which is
    current before trusting either.

**⚠ Repository drift — READ BEFORE CONTINUING (verified 2026-09-15):**
The validation thread above (items 9–12) was done in the **Documents
mirror** (`/var/home/j/Documents/GitHub/cloudy-af`) and its artifacts are
**NOT in this primary repo**: `scripts/audit-cave.py`,
`scripts/uhid_ldrom.py`, `src-tauri/src/bin/anim_patch_dump.rs`,
`docs/validation-handoff.md` and the four
`docs/superpowers/plans/2026-09-1*.md` plans exist only there, and the
mirror's working tree carries ~1096 insertions of tracked-but-uncommitted
work (including a `1.2.1` CHANGELOG entry and emulator/flasher test
changes) that has never been committed — the mirror is 5+ commits behind
on the release line and has diverged on 41 files. Its files are NOT
identical to this repo's for any path. **Do not blindly push the mirror**:
reconcile it first (decide which tree is authoritative per file, port the
validation artifacts here, then commit). The mirror's
`docs/validation-handoff.md` is the running log for this thread — update it
there in place, and port it here if the primary repo is to own it.

## Open work (next-session focus, by leverage)
> **2026-09-15, execution session:** directives below are being worked NOW in
> this session. Status will be flipped in place as items land. Do not start
> duplicate work; re-read statuses first. (Our session run: this is the
> build.sh/portability session; the fair-safety `clippy/tauri.conf.json(78)`
> slot is claimed and OMITS current fair-safety progress until this same
> CSS-in-xhr-in-electron slot runs, so do NOT start `clippy/tauri.conf.json(78)`
> in any newer session before this one.)

1. ✅ **DONE 2026-09-15 — Fix the i18n violation and land the working tree.**
   `Stats.PuffTimeLabel` translated in all 16 locales (derived from each
   locale's own puff-time terminology); `scripts/check-i18n.py` passes
   (17 locales, 387 keys, all 283 used keys present). Working tree landed
   in logical commits (label+i18n, monitor y-axis, build-tooling cleanup,
   T3 fix — see git log). The y-axis question was resolved in favor of the
   working-tree state (single fixed 0–640 axis), recorded in goals.md.
2. ✅ **DONE 2026-09-15 — AppImage device-detection flicker.** Analysis
   redone in-repo at **`docs/t3-appimage-flicker.md`** (the /tmp report is
   gone for good). Primary root cause FIXED: the Rust backend's blind
   1500 ms pre-connect sleep raced slow sidecar startup (cold page cache
   on first AppImage run) — autoconnect is now event-driven on the
   sidecar's existing `ready` announcement. Remaining follow-ups
   (renderer edge-detection, first-failure-only emission) are listed in
   the report; **on-device verification still pending** (needs the Pico).
3. **✅ DONE 2026-09-15 — `./build.sh` AppImage build repaired + made
   portable** (this session). Root causes found and fixed in sequence:
   (a) `--output appimage` PLUS a second separate `appimagetool` call
   double-packed the same AppDir and left a stale AppImage that `ls
   *.AppImage | head -1` could pick up — now just one `linuxdeploy
   --output appimage` step; (b) linuxdeploy ran on the HOST, which has no
   `libwebkit2gtk-4.1` on a flatpak-centric host → "Could not find
   dependency" abort — now runs inside the build toolbox (`CLOUDY_TOOLBOX`
   env, default `arcticfox-build`, with auto-detect + host fallback);
   (c) the script replaced the bundled `strip` with the HOST strip, whose
   `libbfd` does not exist inside the toolbox — that caused the
   `exit 127` crash, now removed; (d) the desktop entry's `Icon=cloudy-af`
   never matched the `Cloudy AF.png` file → whole-build abort — now
   bundles the lowercase icon at the PNG's actual dimensions
   (parsed from the IHDR header) plus a 64×64 fallback; (e) **WebKit
   helper processes were never bundled** — linuxdeploy's gtk plugin does
   NOT copy `/usr/libexec/webkit2gtk-4.1/{WebKitNetworkProcess,
   WebKitWebProcess, WebKitGPUProcess}` (and jsc/MiniBrowser), so the
   AppImage died at startup with "Failed to spawn child process
   .../WebKitNetworkProcess (No such file or directory)" — now copied from
   the correct FOX directory with a `WEBKIT_EXEC_PATH` apprun hook; (f)
   `dist/` was never rebuilt, so the binary embedded a STALE frontend
   (this is why the old script would have silently shipped the blank Puff
   Time labels in 16 locales) — now rebuilt when sources are newer;
   (g) non-interactive CLI so the script is usable in CI/other machines
   (`./build.sh appimage -y`, `--patches`). Tooling portability: toolbox
   prerequisite tools auto-installed via dnf when missing, linuxdeploy
   auto-extracts from the cached AppImage if the extracted tree is missing,
   workdir is under `$HOME` with an EXIT trap, and the produced AppImage is
   searched for in both the workdir and the repo root (appimagetool writes
   it to the invocation cwd, which is NOT necessarily `$WORK_DIR`).
   **`node-hid` build tree artifacts (`build/Release/obj` + `obj.target`)
   are now pruned from the bundled copy** so linuxdeploy stops printing
   "Call to patchelf failed: wrong ELF type" for the `.o` files in every
   log. **Verified end-to-end: `./build.sh appimage -y` succeeds (147 MB
   ELF) and the artifact launches —** the WebKit helpers start, the main
   process and the sidecar come up, and the sidecar runs `hid-bridge.js`.
   **On-device (Pico-connected) verification of the flicker fix is still
   outstanding** (needs the Pico; root cause is fixed in the appliance-only
   code path, but the fix has not been run against live hardware).
4. **Firmware Editor Strings/Resource Packs tabs** — code already exists
   (`refreshStringsTab` renderer-firmware.js:1369, `refreshResourcePacksTab`
   :1638; backend `resources.rs` listStringsCmd/listResourcePacks). ONLY
   remaining work: functional test with a firmware fixture (glyph editor,
   pack preview, extract/inject round-trip vs NFirmwareEditor parity).
   Fixtures: `test-fixtures/`.
4. **Patches tab feature testing** — behind `local-flags.json`
   (`{"patches": true}`: ON locally, OFF on GitHub builds). Test
   end-to-end, then decide whether to default it on for 1.2.1.
5. **macOS GitHub Actions build** — arcticfox patch-version fix
   (`arcticfox@^11.0.3` + `arcticfox+11.0.3.patch`) is pushed to the
   Documents mirror with `.github/workflows/macos-build.yml`; re-run the
   workflow to confirm green. Context: `docs/macos-ci.md`,
   `docs/macos-ci-forgejo-fallback.md`, `docs/macos-job-logs.txt`.
6. **Dependabot glib::VariantStrIter alert** — blocked on upstream Tauri
   (2.11.5 pins gtk 0.18 → glib 0.18.5); do NOT force `[patch.crates-io]`.
   Monitor only. Full write-up in goals.md.
7. **Housekeeping (optional):** todo.md residual items (SDK deep-dive,
   response-pattern refinement); sweep untracked clutter from the repo
   root into `scratch-stash/` or delete. (Stray corrupt `.py…` file
   removed 2026-09-15.)

## Environment cheat-sheet (full detail in AGENTS.md)
- Rust builds: `toolbox run -c arcticfox-build sh -c 'cd src-tauri &&
  PKG_CONFIG_PATH=/usr/lib64/pkgconfig:/usr/share/pkgconfig cargo build
  --release --features tauri/custom-protocol'`
- Flatpak: always `flatpak-builder --force-clean --disable-rofiles-fuse
  --repo=repo build-dir flatpak/org.cloudy.af-local.yml`; stale `build-dir/`
  (the `opendir(refs/heads)` error) → delete `build-dir/` and rebuild.
- After any sidecar `npm install`: rebuild node-hid with the toolbox node
  v22 (exact command in AGENTS.md) or the sidecar SIGABRTs.
- AppImage verification on this FUSE-less host:
  `APPIMAGE_EXTRACT_AND_RUN=1 ./builds/…AppImage`.
- i18n gate before any commit: `python3 scripts/check-i18n.py`
  (17 locales, identical key counts, every `data-lang` key in `en.json`).

## Delegation policy (AGENTS.md, condensed)
Local models ONE AT A TIME (RTX 4050 6 GB VRAM; qwen3-coder:latest primary,
qwen2.5-coder:7b light). `cline` CLI for simple well-specified tasks, ollama
(127.0.0.1:11434) for drafting/review. Local delegates may run alongside a
cloud session. Queue sequentially via one runner script (`setsid nohup`).
Benchmarks: one test at a time, `keep_alive:0`.

## Suggested skills (invoke via the skills tool)
- `codebase-memory` — graph discovery before touching src/ or src-tauri/
  (project `var-home-j-cloudy-af` is current; prefer search_graph /
  trace_path / get_code_snippet over grep).
- `reproducing-bug` — AppImage device-detection flicker (open work #2).
- `browser-automation` — Playwright UI review per REVIEW_DIRECTIVE.md and
  the blank-label i18n DOM checks.
- `writing-tests` — Firmware Editor fixture tests (open work #3).
- `code-audit` — review the uncommitted diff before committing (open work #1).

## Gotchas
- `getLocale()` returns BCP-47 (`de-DE`); always `.substr(0, 2)` before
  building `i18n/<locale>.json` paths.
- Missing `data-lang` keys render as EMPTY text, not raw key names.
- Czech twins `cs.json`/`cz.json` are updated together, always.
- Flash paths expect DECRYPTED .bin (decrypt-on-select bundled; goal marked
  done — do not regress).
- Screenshots (0xC1) respond only with ArcticFox on the device; stock
  firmware silently drops unknown opcodes (see goals.md).
- Emulation/render tests (`test_af_190602_render_gate`,
  `test_af_190602_animation_frames`) are `#[ignore]` — they need the
  gitignored decrypted firmware artifact; do not un-ignore for CI.
